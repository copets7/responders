package com.example.responders.repository;

import com.example.responders.models.SuperCapacitor;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SuperCapacitorRepository extends JpaRepository<SuperCapacitor,Long> {
}
